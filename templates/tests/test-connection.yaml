apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aj-blogs.fullname" . }}-test-connection"
  labels:
    {{- include "aj-blogs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aj-blogs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
