apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aj-blogs.fullname" . }}
  labels:
    app: {{ include "aj-blogs.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "aj-blogs.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "aj-blogs.fullname" . }}
    spec:
      containers:
        - name: {{ include "aj-blogs.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.targetPort }}